% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/m2_pl1_reformat_mdata.R
\name{combine_obslbs}
\alias{combine_obslbs}
\title{Combined observed labels of multiple models into a list.}
\usage{
combine_obslbs(..., byrow = FALSE, prefix = NULL)
}
\arguments{
\item{...}{Multiple datasets. They can be vectors, arrays, data frames,
and lists.}

\item{byrow}{Column vectors are used when matrix, data.frame, or array is used.}

\item{prefix}{Prefix used to name models/classifiers. Serial numbers are
automatically added to prefix to make unique names.}
}
\value{
\code{combine_scores} returns a list that
  contains all combined datasets.
}
\description{
\code{combine_obslbs} takes observed labels and converts them to a list.
It takes several types of datasets, such as vectors, arrays, data frames,
and lists.
}
\examples{
l1 <- c(1, 0, 1, 1)
l2 <- c(1, 1, 0, 0)
l3 <- c(0, 1, 0, 1)
mobslbs <- combine_obslbs(l1, l2, l3)

mobslbs
}

