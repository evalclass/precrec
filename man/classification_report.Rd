% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_classification_report.R
\name{classification_report}
\alias{classification_report}
\title{Classification report}
\usage{
classification_report(
  mdat,
  scores = NULL,
  labels = NULL,
  at = NULL,
  zero_division = 0,
  ...
)
}
\arguments{
\item{mdat}{An \code{mdata} object created by \code{\link[=mmdata]{mmdata()}}. It can be omitted
when \code{scores} and \code{labels} are given.}

\item{scores}{A numeric vector, matrix, array, data frame, or list of
scores. See \code{\link[=mmdata]{mmdata()}} for the accepted shapes.}

\item{labels}{A numeric, character, logical, or factor vector of
observed labels, or a list of such vectors.}

\item{at}{The operating point, as a score threshold. An observation is
predicted positive for a class when its score for that class is greater
than or equal to the threshold. It has no default and must be given;
see the note below on why. Either

\itemize{
\item{a single number, used for every class, or}
\item{one number per class, named after the classes or given in
their order.}
}

A score of \code{NA} is never predicted positive, matching the default
\code{na_worst = TRUE} of the rest of the package.}

\item{zero_division}{The value reported when a precision, recall or
F-score divides by zero - a class nothing was predicted into, or a
class with no observations. \code{0} by default, as in \code{scikit-learn}.
Use \code{NA} for the convention the per-cutoff metrics of \code{\link[=evalmod]{evalmod()}}
follow.}

\item{...}{Further arguments passed to \code{\link[=mmdata]{mmdata()}} when \code{scores} and
\code{labels} are given instead of \code{mdat}.}
}
\value{
A data frame with one row per class and one row per summary,
and the columns

\tabular{ll}{
\code{modnames} \tab Model name \cr
\code{dsids} \tab Dataset ID \cr
\code{class} \tab Class name, or the name of a summary row \cr
\code{precision} \tab Predicted positives that are positive \cr
\code{recall} \tab Positives that are predicted positive \cr
\code{fscore} \tab Harmonic mean of the two \cr
\code{support} \tab Observations of the class \cr
}

The object also has the class \code{classification_report}, which only
affects how it prints; it is a data frame in every other respect.
}
\description{
\code{classification_report} builds the per-class table of precision, recall
and F-score that \code{scikit-learn}'s \code{classification_report} prints, together
with the summary rows that go under it.
}
\section{The summary rows}{


\verb{macro avg} is the unweighted mean over the classes and \verb{weighted avg}
the mean weighted by support. The third row depends on whether the
predictions assign each observation to exactly one class, which is the
rule \code{scikit-learn} documents:

\itemize{
\item{A binary problem at a threshold predicts each observation into
exactly one of the two classes, so the row is \code{accuracy}.}
\item{A multi-class problem is evaluated by one-vs-rest, and each
class is thresholded on its own, so an observation can fall into no
class or into several. There is then no single-label accuracy, and
the row is \verb{micro avg}: precision, recall and F-score recomputed
from the true positives, false positives and false negatives pooled
over the classes.}
}
}

\section{Why \code{at} has no default}{


\code{scikit-learn} reports on \code{y_pred}, so the caller has already chosen an
operating point before the function is called. \code{precrec} holds scores and
evaluates every cutoff, so the report has to be told which one to use, and
the answer changes the table: on the three-class \code{C3N150} the F-score of
one class moves from 0.37 to 0.51 between two reasonable choices. Scores
in \code{precrec} are on whatever scale the classifier produced, so there is no
threshold that is meaningful for all of them - \code{0.5} says nothing about a
log-odds or an SVM margin. The threshold is therefore always the caller's.
}

\examples{

## Multi-class: one row per class, then micro, macro and weighted averages
data(C3N150)
mdat <- mmdata(C3N150$scores, C3N150$labels)
classification_report(mdat, at = 0.5)

## A threshold per class, named or in class order
classification_report(mdat, at = c(c1 = 0.4, c2 = 0.5, c3 = 0.6))

## Binary: both classes, and an accuracy row rather than a micro average
data(P10N10)
classification_report(scores = P10N10$scores, labels = P10N10$labels,
  at = 12
)

## It is a data frame, so the usual accessors work
report <- classification_report(mdat, at = 0.5)
report[report$class == "macro avg", ]

}
\seealso{
\code{\link[=evalmod]{evalmod()}} for the same metrics at every cutoff,
\code{\link[=auc]{auc()}} for the threshold-free summaries and their macro averages,
and \code{\link[=prob_metrics]{prob_metrics()}} for the probability-based losses.
}
