% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_print.R
\name{print}
\alias{print}
\alias{print.mdat}
\alias{print.curve_info}
\alias{print.beval_info}
\alias{print.aucroc}
\alias{print.xycurve_info}
\alias{print.classification_report}
\title{Print the summary of a precrec object}
\usage{
\method{print}{mdat}(x, ...)

\method{print}{curve_info}(x, ...)

\method{print}{beval_info}(x, ...)

\method{print}{aucroc}(x, ...)

\method{print}{xycurve_info}(x, ...)

\method{print}{classification_report}(x, digits = 2, ...)
}
\arguments{
\item{x}{An \code{S3} object created by \code{\link[=mmdata]{mmdata()}}, \code{\link[=evalmod]{evalmod()}},
\code{\link[=metric_curve]{metric_curve()}} or \code{\link[=classification_report]{classification_report()}}. The \code{print} function
takes one of the following \code{S3} objects.\tabular{ll}{
   \strong{\code{S3} object} \tab \strong{Created by} \cr
   \code{mdat} \tab \code{\link[=mmdata]{mmdata()}} \cr
   \code{curve_info} \tab \code{\link[=evalmod]{evalmod()}} \cr
   \code{beval_info} \tab \code{evalmod(mode = "basic")} \cr
   \code{aucroc} \tab \code{evalmod(mode = "aucroc")} \cr
   \code{xycurve_info} \tab \code{\link[=metric_curve]{metric_curve()}} \cr
   \code{classification_report} \tab \code{\link[=classification_report]{classification_report()}} \cr
}


Every object but a \code{classification_report} includes a summary of the
input data - the model names, the dataset IDs and the class counts.
Alongside it, a curve object reports its AUCs, and its partial AUCs
as well when it came from \code{\link[=part]{part()}}; a basic-metric object reports
what each metric abbreviation means and a five-number summary of
every metric; an \code{aucroc} object reports the AUCs beside the U
statistics they came from; and a \code{\link[=metric_curve]{metric_curve()}} object names the
metric pair and counts the points on it. A \code{classification_report}
prints its own table of per-class precision, recall and F-score.

The curve and point objects carry a second class naming how many
models and test datasets they hold, such as \code{sscurves} or \code{mmpoints},
but all of them print through \code{curve_info} or \code{beval_info}. See the
\strong{Value} section of \code{\link[=evalmod]{evalmod()}}.}

\item{...}{Not used by these methods.}

\item{digits}{The number of digits after the decimal point, between \code{0}
and \code{20}. Used by the \code{classification_report} method only.}
}
\value{
The \code{print} function returns \code{x} invisibly.
}
\description{
The \code{print} function prints a summary of an \code{S3} object created by
\code{\link[=mmdata]{mmdata()}}, \code{\link[=evalmod]{evalmod()}}, \code{\link[=metric_curve]{metric_curve()}} or
\code{\link[=classification_report]{classification_report()}}. It is called for its side effect, and is
what the console shows when one of those objects is evaluated at the
prompt.
}
\examples{

##################################################
### Input data
###

## Load a dataset with 10 positives and 10 negatives
data(P10N10)

mdat <- mmdata(P10N10$scores, P10N10$labels)
mdat


##################################################
### ROC and Precision-Recall curves
###

curves <- evalmod(mdat)
curves

## Partial curves also report the partial AUCs
part(curves, xlim = c(0, 0.25))


##################################################
### Basic evaluation metrics
###

points <- evalmod(mdat, mode = "basic")
points


##################################################
### AUC with the U statistic
###

evalmod(mdat, mode = "aucroc")


##################################################
### One metric against another
###

metric_curve(mdat)


##################################################
### Per-class precision, recall and F-score
###

classification_report(mdat, at = 12)

}
\seealso{
\code{\link[=evalmod]{evalmod()}} and \code{\link[=mmdata]{mmdata()}} for creating the objects,
\code{\link[=as.data.frame]{as.data.frame()}} for the same results as a data frame, and
\code{\link[=auc]{auc()}} for the AUCs alone.
}
