% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/g_autoplot4_sm.R
\name{autoplot.smcurves}
\alias{autoplot.smcurves}
\title{Plot ROC and Precision-Recall curves for a single model with multiple datasets}
\usage{
\method{autoplot}{smcurves}(object, curvetype = c("ROC", "PRC"),
  ret_grob = FALSE, ...)
}
\arguments{
\item{object}{\code{smcurves} object generated by
  \code{\link{evalcv}}.}

\item{curvetype}{a character vector with the following curve types
  \describe{
    \item{"ROC"}{ROC curve}
    \item{"PRC"}{Precision-Recall curve}
    \item{c("ROC", "PRC")}{ROC and Precision-Recall curves}
  }}

\item{ret_grob}{A boolean value to indicate whether
  \code{autoplot} returns a frame grob object.}

\item{...}{Not used by this method.}
}
\value{
The \code{autoplot} function returns a \code{ggplot} object
  for a single plot and a frame grob object for multiple plots.
}
\description{
The \code{autoplot} function plots ROC and Precion-Recall curves.
}
\examples{
## Load libraries
library(ggplot2)
library(grid)
library(gridExtra)

## Prepare input data
samps <- create_sim_samples(10, 100, 100, "poor_er")
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
               model_names = samps[["model_names"]],
               setids = samps[["setids"]])

## Generate an mscurve object
curves <- evalcv(mdat)

## Plot both ROC and Precision-Recall curves
autoplot(curves)

## Plot a ROC curve
autoplot(curves, curvetype = "ROC")

## Plot a Precision-Recall curve
pp1 <- autoplot(curves, curvetype = "PRC")
pp1

## autoplot returns a grob object.
pp2 <- autoplot(curves, ret_grob = TRUE)
plot.new()
grid.draw(pp2)
}
\seealso{
\code{\link{evalcv}} for generating \code{smcurves}.

\code{\link{evalcv}} for generating \code{smcurves}.
 \code{\link{fortify.smcurves}} for converting \code{smcurves} to list.
 \code{\link{plot.smcurves}} for plotting the same curves
  with the general R plot.
}

