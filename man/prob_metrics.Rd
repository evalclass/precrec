% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_prob_metrics.R
\name{prob_metrics}
\alias{prob_metrics}
\title{Calculate the Brier score, the RMSE and the log loss of predicted
probabilities}
\usage{
prob_metrics(mdat, scores = NULL, labels = NULL, eps = 1e-15, ...)
}
\arguments{
\item{mdat}{An \code{S3} object created by the \code{\link[=mmdata]{mmdata()}} function.
It contains formatted scores and labels. The \code{prob_metrics}
function ignores \code{scores} and \code{labels} when \code{mdat}
is specified. These arguments are internally passed to the
\code{\link[=mmdata]{mmdata()}} function when \code{mdat} is unspecified. In that case,
both \code{scores} and \code{labels} must be at least specified.}

\item{scores}{A numeric dataset of predicted probabilities. It can be a
vector, a matrix, an array, a data frame, or a list.}

\item{labels}{A numeric, character, logical, or factor dataset
of observed labels. It can be a vector, a matrix, an array,
a data frame, or a list.}

\item{eps}{A numeric value used to clamp the scores away from \code{0}
and \code{1} before the log loss is calculated. A single confident and
wrong prediction would otherwise make the log loss infinite.}

\item{...}{These additional arguments are passed to \code{\link[=mmdata]{mmdata()}}
for data preparation.}
}
\value{
The \code{prob_metrics} function returns a data frame with the
columns \code{modnames}, \code{dsids}, \code{metrics}, and
\code{values}. \code{metrics} is one of "brier", "rmse" or
"logloss", so each model and dataset combination takes up three
rows.
}
\description{
The \code{prob_metrics} function calculates three probability-based
evaluation metrics - the Brier score, its square root the root mean
squared error, and the log loss - for prediction scores that are
probabilities. Unlike ROC and Precision-Recall curves, these metrics
depend on the values of the scores rather than on their ranks, so the
scores must lie in the range [0, 1].
}
\examples{

##################################################
### Single model & single test dataset
###

## Predicted probabilities of 10 positives and 10 negatives
set.seed(1)
scores <- c(runif(10, 0.4, 1), runif(10, 0, 0.6))
labels <- c(rep(1, 10), rep(0, 10))

## Brier score and log loss
prob_metrics(scores = scores, labels = labels)


##################################################
### Multiple models & multiple test datasets
###

## The "poor_er" and "good_er" samples are drawn from beta distributions,
## so their scores can be read as probabilities
samps <- create_sim_samples(4, 100, 100, c("poor_er", "good_er"))
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]],
  dsids = samps[["dsids"]]
)

pm <- prob_metrics(mdat)

## Show the Brier scores only
subset(pm, metrics == "brier")

}
\seealso{
\code{\link[=prob_metrics_ci]{prob_metrics_ci()}} for the CIs of these metrics over multiple
datasets. \code{\link[=evalmod]{evalmod()}} for generating \code{S3} objects with
performance evaluation measures. \code{\link[=mmdata]{mmdata()}} for formatting input data.
}
