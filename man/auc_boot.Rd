% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_auc_boot.R
\name{auc_boot}
\alias{auc_boot}
\title{Bootstrap AUCs from one test set}
\usage{
auc_boot(mdat, scores = NULL, labels = NULL, boot_n = 1000, seed = NULL, ...)
}
\arguments{
\item{mdat}{An \code{mdata} object created by \code{\link[=mmdata]{mmdata()}} holding exactly one
dataset. It can be omitted when \code{scores} and \code{labels} are given.}

\item{scores}{A numeric vector, matrix, array, data frame, or list of
scores. See \code{\link[=mmdata]{mmdata()}} for the accepted shapes.}

\item{labels}{A numeric, character, logical, or factor vector of observed
labels, or a list of such vectors.}

\item{boot_n}{The number of bootstrap resamples. The default of 1000 is
the usual compromise; the tails of a percentile interval are estimated
from \code{alpha / 2} of these, so 25 resamples at each end of a 95\%
interval, and 2000 or more is worth the wait when the interval itself
is the result being reported.}

\item{seed}{A seed for the resampling, so that a reported interval can be
reproduced. The global random number state is restored afterwards, so a
seeded call does not disturb the stream the caller is drawing from.
\code{NULL}, the default, resamples from the current state.}

\item{...}{Further arguments passed to \code{\link[=mmdata]{mmdata()}} when \code{scores} and
\code{labels} are given instead of \code{mdat}.}
}
\value{
An object of class \code{aucboot}: a data frame of one row per model,
curve type and resample, with the columns \code{modnames}, \code{curvetypes},
\code{boot_id} and \code{aucs}. The AUCs of the original data are attached as the
\code{observed} attribute.
}
\description{
\code{auc_boot} resamples a single test set and recalculates the areas under
the ROC and precision-recall curves on each resample. The result carries
the whole bootstrap distribution, which \code{\link[=auc_ci]{auc_ci()}} turns into confidence
intervals and \code{\link[=auc_diff]{auc_diff()}} into comparisons between models.
}
\details{
The rest of the package builds its intervals from the variation between
several test sets, so \code{\link[=auc_ci]{auc_ci()}} on an \code{evalmod} result needs several and
says so. This is the answer for the ordinary case of one test set, where
the only sample there is has to stand in for the population.
}
\section{Resampling}{


The resampling is \strong{stratified}: positives are drawn from the positives
and negatives from the negatives, so every resample holds exactly the
class balance the original does. An unstratified bootstrap of an
imbalanced dataset produces resamples with a different balance, and
sometimes with no positives at all, which moves the precision-recall
baseline underneath the quantity being estimated.

Every model is resampled on the \strong{same} draws. Comparisons between
models are then paired, which is what makes \code{\link[=auc_diff]{auc_diff()}} a statement
about the difference rather than about two independent intervals.
}

\examples{

## One test set, two models
samps <- create_sim_samples(1, 100, 100, c("poor_er", "good_er"))
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]]
)

booted <- auc_boot(mdat, boot_n = 200, seed = 42)
auc_ci(booted)
auc_diff(booted)

}
\seealso{
\code{\link[=auc_ci]{auc_ci()}} for the intervals, \code{\link[=auc_diff]{auc_diff()}} for comparing two
models, and \code{\link[=auc]{auc()}} for the point estimates.
}
