% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main_best_cutoff.R
\name{best_cutoff}
\alias{best_cutoff}
\title{Choose an operating point}
\usage{
best_cutoff(x, scores = NULL, labels = NULL, metric = "youden", ...)
}
\arguments{
\item{x}{An \code{S3} object created by the \code{\link[=mmdata]{mmdata()}} function, or a
basic-metric object created by \code{evalmod(mode = "basic")}. The
\code{best_cutoff} function ignores \code{scores} and \code{labels} when \code{x} is
specified. These arguments are internally passed to the \code{\link[=mmdata]{mmdata()}}
function when \code{x} is unspecified. In that case, both \code{scores} and
\code{labels} must be at least specified.}

\item{scores}{A numeric dataset of predicted scores. It can be a vector,
a matrix, an array, a data frame, or a list.}

\item{labels}{A numeric, character, logical, or factor dataset
of observed labels. It can be a vector, a matrix, an array,
a data frame, or a list.}

\item{metric}{A string naming the metric to optimize. It can be any of
the metrics \code{\link[=evalmod]{evalmod()}} calculates, together with two names that come
from the cutpoint literature rather than from the metric table.

\tabular{lll}{
\strong{Name} \tab \strong{Metric} \tab \strong{Optimum} \cr
\code{"youden"} \tab \code{informedness} \tab Maximum \cr
\code{"topleft"} \tab \code{roc_dist} \tab Minimum \cr
\code{"fscore"} \tab \code{fscore} \tab Maximum \cr
\code{"mcc"} \tab \code{mcc} \tab Maximum \cr
\code{"cost"} \tab \code{cost} \tab Minimum \cr
}

The direction is a property of the metric, so it is not an argument -
\code{error}, \code{cost}, \code{roc_dist}, the two error rates and the negative
likelihood ratio are minimized, and the rest are maximized. \code{score},
\code{label}, \code{predicted_positive_rate} and \code{predicted_negative_rate}
describe a cutoff rather than score it, and are refused.}

\item{...}{These additional arguments are passed to \code{\link[=evalmod]{evalmod()}}, and
through it to \code{\link[=mmdata]{mmdata()}}, when this function builds the metrics
itself. \code{beta} for \code{"fscore"}, \code{cost_fp} and \code{cost_fn} for \code{"cost"},
and \code{basic_ties}, \code{modnames}, \code{dsids} and \code{posclass} are the useful
ones here.}
}
\value{
The \code{best_cutoff} function returns a data frame with one row per
model per test dataset, and the following columns.

\tabular{ll}{
\code{modname} \tab Model name \cr
\code{dsid} \tab Test dataset ID \cr
\code{metric} \tab The metric that was optimized \cr
\code{value} \tab Its value at the chosen cutoff \cr
\code{rank} \tab Number of instances called positive, \code{1} to \code{n} \cr
\code{normalized_rank} \tab \code{rank / n} \cr
\code{score} \tab The cutoff itself, the rule being \verb{score >= } it \cr
\code{label} \tab \code{1} if the instance at this rank is positive,
\code{-1} if it is negative \cr
... \tab One column per metric, as \code{\link[=metric_table]{metric_table()}} returns them \cr
}

The row is a row of \code{\link[=metric_table]{metric_table()}}, so every other metric is there
to be read at the same cutoff - which is the point of returning the
whole row rather than the threshold alone.
}
\description{
The \code{best_cutoff} function picks the cutoff that optimizes one
evaluation metric, in the manner of \code{pROC::coords(x, "best")}. It
returns one row per model per test dataset - a row of
\code{\link[=metric_table]{metric_table()}}, together with the criterion that chose it.
}
\section{The criterion matters more than the threshold}{


Youden's J and the closest point to the top left corner are the two
criteria most often reached for, and both are computed from sensitivity
and specificity alone. Both of those are conditioned on the true class,
so neither knows the prevalence, and on imbalanced data the cutoff they
choose can sit at a precision no one would deploy. That is the argument
this package makes about the ROC curve, one step further down the
pipeline.

\tabular{ll}{
\strong{Prevalence-blind} \tab \code{"youden"}, \code{"topleft"} \cr
\strong{Prevalence-aware} \tab \code{"fscore"}, \code{"mcc"}, \code{"cost"} \cr
}

The default is \code{"youden"} because that is what a caller arriving from
another package expects. On imbalanced data it is the wrong default, and
\code{"mcc"} or a \code{"cost"} weighted by what the two mistakes actually cost is
the better choice. The \emph{Balanced and imbalanced data} article shows the
two disagreeing on the same dataset:
\url{https://evalclass.github.io/precrec/articles/howto-imbalanced-data.html}.
}

\section{Ties, and what is not here}{


Several cutoffs can share the optimum. The one with the smallest \code{rank}
is returned - the threshold that calls the fewest instances positive -
so the result is one row per model per test dataset whatever the data
does.

Rows that share a \code{score} are an exception, because they are one
threshold seen several times rather than several cutoffs: \verb{score >= }
that value calls all of the tied instances positive, so the \code{rank}
reported is the last of the run and not the first. That is what
\code{evalmod(basic_ties = "hold")} produces throughout, since it gives every
cutoff in a run of tied scores the counts of the whole run.

The rank \code{0} row of \code{\link[=metric_table]{metric_table()}} - the rule that calls nothing
positive - is not a candidate. There is no threshold that predicts
nothing, so its \code{score} is \code{NA} and it is not an operating point, which
is what this function returns. It would otherwise win outright wherever
the empty rule is optimal, and win on the tie-break wherever it merely
ties: precision at rank \code{0} is the limit from above, so it is \code{1}
whenever the top-ranked instance is a positive, and any classifier that
ranks one first would come back with no threshold at all.

A metric that has no interior optimum is still optimized at an end of the
range, and no warning says so: \code{sensitivity} is largest when everything
is called positive, and \code{specificity} when as little as possible is.
Those are the correct answers to the question asked, and rarely the
question meant. \code{accuracy} and \code{error} have an interior optimum on
balanced data and lose it as positives get rare, which is the same trap
arriving by a different route: at a few percent positives, calling almost
nothing positive is close to the most accurate thing a classifier can do.

A cutoff chosen on the same data the model is evaluated on is optimistic,
by however much the criterion was free to chase. Choosing it on held-out
data is the fix, and it is the caller's to make: nothing here resamples
or cross-validates the choice.
}

\examples{

##################################################
### Single model & single test dataset
###

## Load a dataset with 10 positives and 10 negatives
data(P10N10)

## The cutoff that maximizes Youden's J
best_cutoff(scores = P10N10$scores, labels = P10N10$labels)


##################################################
### A criterion that knows the prevalence
###

best_cutoff(scores = P10N10$scores, labels = P10N10$labels, metric = "mcc")

## Weighted by what the two mistakes cost
best_cutoff(
  scores = P10N10$scores, labels = P10N10$labels,
  metric = "cost", cost_fp = 1, cost_fn = 5
)


##################################################
### Multiple models & multiple test datasets
###

samps <- create_sim_samples(2, 50, 50, c("poor_er", "good_er"))
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]], dsids = samps[["dsids"]]
)
best_cutoff(mdat, metric = "fscore")[, 1:7]

}
\seealso{
\code{\link[=metric_table]{metric_table()}} for every metric at every cutoff, which this
function takes one row of, \code{\link[=evalmod]{evalmod()}} for calculating the metrics,
and \code{\link[=prbe]{prbe()}} for the precision-recall break-even point, which is an
operating point defined by a crossing rather than by an optimum.
}
