% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/m2_pl4_create_mcurves.R
\name{create_mcurves}
\alias{create_mcurves}
\title{Create ROC and Precision-Recall curves for multiple models.}
\usage{
create_mcurves(mdat, mscores = NULL, mobslabs = NULL, x_interval = 0.001,
  model_names = NULL, ...)
}
\arguments{
\item{mdat}{An \code{mdat} object created by \code{\link{create_mdat}}.}

\item{mscores}{A dataset of predicted scores.}

\item{mobslabs}{A dataset of of observed labels.}

\item{x_interval}{A numeric value to specifiy an interval of the
x-axis (TPRs for ROC and recall for Precision-Recall).}

\item{model_names}{Names of the models/classifiers to be evaluated.}

\item{...}{Other arguments passed to \code{\link{reformat_data}}}

\item{list_curves}{A list of  \code{curves} objects created by
\code{\link{create_curves_for_multi}}.}
}
\value{
\code{create_mprc_curves} returns a \code{mprc_curves}
S3 objects that contains Precision-Recall curves for multiple models.
}
\description{
\code{create_mcurves} takes input data as three different options
as below.
\enumerate{
  \item \code{list_curves} only
  \item \code{mdat} only
  \item Both \code{mscores} and \code{mobslabs}
}
\code{list_curves} must be a list that contains ROC and Precision-Recall
curves for multiple models. \code{mdat} should be an S3 object that contains
predicted scores from multiple models and corresponding binary lables
from an observed dataset. \code{create_mcurves} can also take
datasets of scores and labels as \code{mscores} and \code{mobslabs}.
It returns a \code{mcurves} that contains ROC and Precision-Recall curves
for multiple models.
}
\examples{
s1 <- c(1, 2, 3, 4)
s2 <- c(5, 6, 7, 8)
s3 <- c(2, 4, 6, 8)
mscores <- combine_scores(s1, s2, s3)

l1 <- c(1, 0, 1, 1)
l2 <- c(1, 1, 0, 0)
l3 <- c(0, 1, 0, 1)
mobslabs <- combine_obslbs(l1, l2, l3)

mdat <- create_mdat(mscores, mobslabs)
list_curves <- create_curves_for_multi(mdat)

mcurves <- create_mcurves(list_curves)
mcurves
}

