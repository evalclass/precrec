% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_auc_boot.R, R/g_auc_delong.R
\name{auc_diff}
\alias{auc_diff}
\alias{auc_diff.aucboot}
\alias{auc_diff.aucdelong}
\title{Compare AUCs between models}
\usage{
auc_diff(x, alpha = NULL, alternative = NULL)

\method{auc_diff}{aucboot}(x, alpha = 0.05, alternative = "two.sided")

\method{auc_diff}{aucdelong}(x, alpha = 0.05, alternative = "two.sided")
}
\arguments{
\item{x}{An \code{aucboot} object from \code{\link[=auc_boot]{auc_boot()}} or an \code{aucdelong} object
from \code{\link[=auc_delong]{auc_delong()}}.}

\item{alpha}{The interval covers \code{1 - alpha} of the resampled
differences, so the default of 0.05 gives a 95\% interval.}

\item{alternative}{The side of zero the alternative hypothesis is on,
one of \code{"two.sided"}, \code{"greater"} and \code{"less"}. \code{"greater"} tests
whether the first model of the pair has the larger AUC. It selects the
tail of every p-value in the result and leaves \code{z_values} alone; the
interval stays two-sided whatever it is set to.}
}
\value{
A data frame with one row per curve type and pair of models.

From an \code{aucboot} object:

\tabular{ll}{
\code{curvetypes} \tab ROC or PRC \cr
\code{modnames1}, \code{modnames2} \tab The pair, in the order compared \cr
\code{diffs} \tab Observed AUC of the first minus that of the second \cr
\code{lower_bound}, \code{upper_bound} \tab Percentile interval of the
resampled differences \cr
\code{p_values} \tab Percentile p-value, see below \cr
\code{z_values} \tab Wald statistic, \code{diffs / sd(d)}, see below \cr
\code{p_values_wald} \tab Wald p-value, see below \cr
\code{n} \tab Resamples the row is based on \cr
}

From an \code{aucdelong} object, where there is one p-value rather than
two and only the ROC AUC to report:

\tabular{ll}{
\code{curvetypes} \tab ROC \cr
\code{modnames1}, \code{modnames2} \tab The pair, in the order compared \cr
\code{diffs} \tab AUC of the first minus that of the second \cr
\code{lower_bound}, \code{upper_bound} \tab Normal interval of the
difference, clipped to \verb{[-1, 1]} \cr
\code{z_values} \tab \code{diffs} over its standard error \cr
\code{p_values} \tab DeLong's p-value, see below \cr
\code{n} \tab Instances in the test set \cr
}
}
\description{
\code{auc_diff} reports, for every pair of models, the difference between
their AUCs with a confidence interval around it. It reads either of the
two estimates of uncertainty the package offers for a single test set:
the resamples of \code{\link[=auc_boot]{auc_boot()}}, which cover both curve types, or the
analytic covariance of \code{\link[=auc_delong]{auc_delong()}}, which covers the ROC AUC exactly.
}
\details{
The comparison is paired either way. With a bootstrap both models are
scored on the same resample, so the difference is calculated within a
resample and the spread of those differences is what the interval
describes; with DeLong the pairing is carried by the covariance between
the two AUCs. Two separate intervals from \code{\link[=auc_ci]{auc_ci()}} cannot be read this
way - they can overlap while the difference is still clearly on one side
of zero, because they say nothing about how the two models move
together.
}
\section{DeLong's comparison}{


Given an \code{aucdelong} object the standard error of the difference comes
out of the covariance matrix,

\if{html}{\out{<div class="sourceCode">}}\preformatted{se = sqrt(var(auc1) + var(auc2) - 2 * cov(auc1, auc2))
z_values = diffs / se
p_values = 2 * pnorm(-abs(z_values))
}\if{html}{\out{</div>}}

and the covariance is what makes it a paired test: two models scored on
the same test set rise and fall together, and dropping the last term
would overstate how uncertain their difference is.

This is a Wald test too, so \code{alternative} selects its tail the same way.
What it does not need is \code{boot_n}: the standard error is exact rather
than resampled, so the p-value has no floor and does not move between
two runs. What it does assume is that the variance is asymptotic - see
\code{\link[=auc_delong]{auc_delong()}} - and it has nothing to say about the precision-recall
AUC.

\code{z_values} is \code{NA} when two models rank every instance the same way and
there is no standard error to divide by, and \code{p_values} is \code{NA} with it.
}

\section{The interval and the two p-values}{


This section and the two that follow describe an \code{aucboot} object.

The interval is the primary result, and is the \code{alpha / 2} and
\code{1 - alpha / 2} quantiles of the resampled differences.

The two p-values are the two ways of getting one out of a bootstrap, and
each column is named for the method that produced it. Write \code{d} for the
vector of \code{n} resampled differences and \code{diffs} for the observed one.

\code{p_values} is the \strong{percentile} p-value - the share of \code{d} on the other
side of zero from \code{diffs}, doubled for a two-sided test:

\if{html}{\out{<div class="sourceCode">}}\preformatted{p_values = 2 * min(1 + sum(d <= 0), 1 + sum(d >= 0)) / (n + 1)
}\if{html}{\out{</div>}}

The added ones keep it away from exactly zero. It cannot go below
\code{2 / (n + 1)}, so \code{boot_n} sets a floor under it.

\code{z_values} and \code{p_values_wald} are the \strong{Wald} test - an estimate over
an estimate of its standard error, referred to a standard normal:

\if{html}{\out{<div class="sourceCode">}}\preformatted{z_values      = diffs / sd(d)
p_values_wald = 2 * pnorm(-abs(z_values))
}\if{html}{\out{</div>}}

\code{sd(d)} is the standard error, because the spread of the bootstrap
distribution is what the bootstrap has to say about how far the
difference moves from sample to sample. Having a scale underneath it
rather than a count of resamples, the Wald p-value has no floor.

\code{alternative} replaces the doubled minimum with the matching one-sided
count, and \code{-abs()} with \code{-} or \code{+}, in the two formulas above.
}

\section{Why two p-values}{


Because they fail in opposite ways, and neither one on its own tells you
that it is failing.

The percentile p-value assumes nothing about the shape of \code{d}, and pays
for that with the floor. Once it reaches \code{2 / (n + 1)} it has stopped
measuring the models and started reporting \code{boot_n}: a difference that is
merely clear and one that is overwhelming both come out at 0.002 at the
default thousand resamples, and the number gives no sign of which it is
looking at.

The Wald p-value has no floor, and pays for that with an assumption. It
takes \code{d} to be roughly normal, and locates the null by reflecting the
sampling distribution rather than by enforcing it. Where \code{d} is skewed -
few positives, or either model near the ceiling of the precision-recall
AUC - it is confidently wrong, and again the number carries no warning.

Side by side they cover each other. When they agree, the normality the
Wald test assumes is doing no harm at this sample size and you can quote
its resolution. When they disagree sharply, \code{d} is not the shape the Wald
test needs, and the percentile p-value - floor and all - is the one to
trust. The comparison is the diagnostic; neither column is one on its
own.

There is a second, exact sense in which they are different answers. A
Wald test is the counterpart of the bootstrap \strong{normal} interval,
\code{diffs} plus and minus \code{z} standard errors, while \code{lower_bound} and
\code{upper_bound} are the \strong{percentile} interval. So \code{p_values_wald} is not
the dual of the bounds reported beside it, and need not agree with them.

Read the interval first. Both p-values are companions to it rather than
exact tests.
}

\section{Not a t statistic}{


\code{z_values} divides by a standard error, not by a standard error of a
mean, and is read off the normal rather than off a t. \code{auc_ci(dtype = "t")} is the genuine t in this package, and the contrast is exact: there
the spread is taken over a handful of real test sets and divided by the
square root of how many there were, so \code{n - 1} degrees of freedom mean
something. Here the spread is taken over resamples and divided by
nothing - \code{boot_n} is a setting rather than a sample size, and a t on
\code{boot_n - 1} degrees of freedom would be a p-value that shrinks when the
caller resamples harder.

Nor is this the bootstrap-\emph{t}, which forms a statistic of its own inside
every resample and takes its reference distribution from those rather
than from the normal.

\code{z_values} is \code{NA} when the resamples have no spread to divide by, which
happens when two models are given the same scores, and \code{p_values_wald}
is \code{NA} with it.
}

\examples{

samps <- create_sim_samples(1, 100, 100, c("poor_er", "good_er"))
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]]
)

booted <- auc_boot(mdat, boot_n = 200, seed = 42)
auc_diff(booted)

## Is the second model the better one?
auc_diff(booted, alternative = "less")

## The same comparison without resampling, for the ROC AUC
auc_diff(auc_delong(mdat))

}
\seealso{
\code{\link[=auc_boot]{auc_boot()}} for the resampling, \code{\link[=auc_delong]{auc_delong()}} for the
analytic alternative, and \code{\link[=auc_ci]{auc_ci()}} for one model at a time.
}
