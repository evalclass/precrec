% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_auc_boot.R
\name{auc_diff}
\alias{auc_diff}
\title{Compare bootstrapped AUCs between models}
\usage{
auc_diff(x, alpha = 0.05, alternative = "two.sided")
}
\arguments{
\item{x}{An \code{aucboot} object from \code{\link[=auc_boot]{auc_boot()}}.}

\item{alpha}{The interval covers \code{1 - alpha} of the resampled
differences, so the default of 0.05 gives a 95\% interval.}

\item{alternative}{The side of zero the alternative hypothesis is on,
one of \code{"two.sided"}, \code{"greater"} and \code{"less"}. \code{"greater"} tests
whether the first model of the pair has the larger AUC. It selects the
tail of both p-values; the interval stays two-sided whatever it is set
to.}
}
\value{
A data frame with one row per curve type and pair of models:

\tabular{ll}{
\code{curvetypes} \tab ROC or PRC \cr
\code{modnames1}, \code{modnames2} \tab The pair, in the order compared \cr
\code{diffs} \tab Observed AUC of the first minus that of the second \cr
\code{lower_bound}, \code{upper_bound} \tab Percentile interval of the
resampled differences \cr
\code{p_values} \tab Percentile p-value, see below \cr
\code{statistic} \tab Wald statistic: \code{diffs} over the standard deviation
of the resampled differences \cr
\code{p_values_norm} \tab \code{statistic} read off the standard normal, see
below \cr
\code{n} \tab Resamples the row is based on \cr
}
}
\description{
\code{auc_diff} takes the resamples of \code{\link[=auc_boot]{auc_boot()}} and reports, for every
pair of models and every curve type, the difference between their AUCs
with a confidence interval around it.
}
\details{
The comparison is paired: both models are scored on the same resample,
so the difference is calculated within a resample and the spread of those
differences is what the interval describes. Two separate intervals from
\code{\link[=auc_ci]{auc_ci()}} cannot be read this way - they can overlap while the
difference is still clearly on one side of zero, because they say nothing
about how the two models move together.
}
\section{The interval and the two p-values}{


The interval is the primary result, and is the \code{alpha / 2} and
\code{1 - alpha / 2} quantiles of the resampled differences.

\code{p_values} is the proportion of resampled differences falling on the
other side of zero from the observed one, doubled for a two-sided test,
and calculated as \code{2 * min(1 + sum(d <= 0), 1 + sum(d >= 0)) / (n + 1)}
so that it is never exactly zero - a bootstrap of \code{n} resamples cannot
report a p-value below about \code{2 / n}, and reporting one would be an
artifact of the resample count rather than evidence.

\code{statistic} is a \strong{Wald statistic}: an estimate over an estimate of its
standard error. The standard deviation of the resampled differences is
that standard error - the spread of the bootstrap distribution is what
the bootstrap has to say about how far the difference moves from sample
to sample - and \code{p_values_norm} reads the statistic off the standard
normal.

It is deliberately not a \emph{t} statistic. \code{auc_ci(dtype = "t")} is one,
because there the spread is taken over a handful of real test sets,
divided by the square root of how many there were, and \code{n - 1} degrees
of freedom mean something. Here the spread is taken over resamples and
divided by nothing: \code{boot_n} is a setting rather than a sample size, and
a t with \code{boot_n - 1} degrees of freedom would let a p-value shrink by
resampling harder. Nor is it the bootstrap-\emph{t}, which forms a statistic
of its own inside every resample and takes its reference distribution
from those rather than from the normal.

The pair is the test counterpart of the bootstrap \strong{normal} interval,
\code{diffs} plus and minus \code{z} standard errors, whereas \code{lower_bound} and
\code{upper_bound} are the \strong{percentile} interval. So \code{p_values_norm} is not
the dual of the bounds next to it in the same row, which is one of the
two reasons the p-values can disagree.

The other is what the normal approximation assumes. Nothing is shuffled
between the models, so the null is never enforced; the distribution is
the sampling one, centered on the observed difference and reflected to
sit under zero. It also takes the resampled differences to be roughly
normal, which is weakest where they are skewed - few positives, or
either model near the ceiling of the precision-recall AUC. The small
p-values it makes available are therefore its least trustworthy numbers.

Read the interval first. The two p-values are companions to it rather
than exact tests, and when they disagree sharply it is the normal
approximation to distrust.

\code{statistic} is \code{NA} when the resamples have no spread to divide by, which
happens when two models are given the same scores, and \code{p_values_norm}
is \code{NA} with it.
}

\examples{

samps <- create_sim_samples(1, 100, 100, c("poor_er", "good_er"))
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]]
)

booted <- auc_boot(mdat, boot_n = 200, seed = 42)
auc_diff(booted)

## Is the second model the better one?
auc_diff(booted, alternative = "less")

}
\seealso{
\code{\link[=auc_boot]{auc_boot()}} for the resampling and \code{\link[=auc_ci]{auc_ci()}} for one model at
a time.
}
