% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_pauc.R
\name{pauc}
\alias{pauc}
\alias{pauc.aucs}
\title{Retrieve a data frame of pAUC scores}
\usage{
pauc(curves, corrected = FALSE)

\method{pauc}{aucs}(curves, corrected = FALSE)
}
\arguments{
\item{curves}{An \code{S3} object generated by \code{\link[=part]{part()}} and
\code{\link[=evalmod]{evalmod()}}. The \code{pauc} function accepts the following
S3 objects.\tabular{lll}{
   \strong{\code{S3} object} \tab \strong{# of models} \tab \strong{# of test datasets} \cr
   sscurves \tab single \tab single \cr
   mscurves \tab multiple \tab single \cr
   smcurves \tab single \tab multiple \cr
   mmcurves \tab multiple \tab multiple \cr
}


See the \strong{Value} section of \code{\link[=evalmod]{evalmod()}} for more details.}

\item{corrected}{A logical value to add the \code{cpaucs} column, the McClish
correction of the ROC partial areas. The default is \code{FALSE}.}
}
\value{
The \code{pauc} function returns a data frame with one row per curve
per model per test dataset, and the following columns. An object that
holds averaged curves only has no \code{dsids} column, because an averaged
curve has no single test dataset behind it.

\tabular{ll}{
\code{modnames} \tab Model name \cr
\code{dsids} \tab Test dataset ID \cr
\code{curvetypes} \tab \code{ROC} or \code{PRC} \cr
\code{paucs} \tab The area under that curve over the region \cr
\code{spaucs} \tab That area over the area of the region \cr
\code{cpaucs} \tab The McClish correction, with \code{corrected = TRUE} \cr
}
}
\description{
The \code{auc} function takes an \code{S3} object generated by
\code{\link[=part]{part()}} and \code{\link[=evalmod]{evalmod()}} and retrieves a data frame
with the partial AUC scores of ROC and Precision-Recall curves.
}
\section{Two ways to standardize a partial area}{


A partial area is not comparable across regions on its own - a wider
region holds more area - so it is reported standardized, and there are
two conventions for doing it. Both are called "the standardized partial
AUC", and a number carried between tools looks like a disagreement when
it is a choice of convention.

\code{spaucs} divides the area by the area of the region, so it says what
fraction of what was available the curve covered. The McClish
correction instead rescales the span between chance and perfect onto
\code{0.5} to \code{1}, so that a partial area reads on the same scale as a full
one. Over false positive rates up to \code{0.2}, a coin flip covers an area
of \code{0.02}: that is \code{0.1} of the region, and \code{0.5} after the correction.

Neither is the right one. \code{spaucs} is the more direct reading of how
much of the corner the curve filled; the corrected one is the more
comparable to a full AUC. Say which you used.

\code{cpaucs} is what \code{pROC::auc(..., partial.auc.correct = TRUE)} returns,
to the last digit. The chance area is taken over the region actually
asked for, \code{(x2^2 - x1^2) / 2}, and not over a region assumed to start
at a false positive rate of \code{0} - the formula usually quoted is the
special case of that one, and gets a region such as
\code{xlim = c(0.1, 0.3)} wrong.

The correction is the identity over the whole curve, where chance is
\code{0.5} and the region is \code{1}, so a \code{cpaucs} from \code{part(xlim = c(0, 1))}
is the plain AUC. At the other end it is unstable: over a narrow region
of high false positive rates, chance and perfect are close together and
the rescaling divides by the small difference, so a curve a little
below the diagonal there can produce a large negative number. That is
the correction behaving as defined, not an error.

\code{cpaucs} is \code{NA} on every \code{PRC} row, and on every row when \code{part()} was
given a \code{ylim} other than \code{c(0, 1)}. A precision-recall curve's chance
level is the proportion of positives rather than the diagonal, and
rescaling by it would make curves at different class balances look
comparable when the point of the precision-recall plot is that they are
not - see \code{\link[=auc]{auc()}}. A restricted \code{ylim} has no counterpart in \code{pROC} and
no settled definition, so it reports nothing rather than invent one.
}

\examples{

##################################################
### Single model & single test dataset
###

## Load a dataset with 10 positives and 10 negatives
data(P10N10)

## Generate an sscurve object that contains ROC and Precision-Recall curves
sscurves <- evalmod(scores = P10N10$scores, labels = P10N10$labels)

## Calculate partial AUCs
sscurves.part <- part(sscurves, xlim = c(0.25, 0.75))

## Shows pAUCs
pauc(sscurves.part)

##################################################
### Multiple models & single test dataset
###

## Create sample datasets with 100 positives and 100 negatives
samps <- create_sim_samples(1, 100, 100, "all")
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]]
)

## Generate an mscurve object that contains ROC and Precision-Recall curves
mscurves <- evalmod(mdat)

## Calculate partial AUCs
mscurves.part <- part(mscurves, xlim = c(0, 0.75), ylim = c(0.25, 0.75))

## Shows pAUCs
pauc(mscurves.part)

##################################################
### Single model & multiple test datasets
###

## Create sample datasets with 100 positives and 100 negatives
samps <- create_sim_samples(4, 100, 100, "good_er")
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]],
  dsids = samps[["dsids"]]
)

## Generate an smcurve object that contains ROC and Precision-Recall curves
smcurves <- evalmod(mdat, raw_curves = TRUE)

## Calculate partial AUCs
smcurves.part <- part(smcurves, xlim = c(0.25, 0.75))

## Shows pAUCs
pauc(smcurves.part)

##################################################
### Multiple models & multiple test datasets
###

## Create sample datasets with 100 positives and 100 negatives
samps <- create_sim_samples(4, 100, 100, "all")
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]],
  dsids = samps[["dsids"]]
)

## Generate an mscurve object that contains ROC and Precision-Recall curves
mmcurves <- evalmod(mdat, raw_curves = TRUE)

## Calculate partial AUCs
mmcurves.part <- part(mmcurves, xlim = c(0, 0.25))

## Shows pAUCs
pauc(mmcurves.part)

##################################################
### The McClish correction
###

## The pAUC rescaled between chance and perfect, as pROC reports it
pauc(sscurves.part, corrected = TRUE)

}
\seealso{
\code{\link[=evalmod]{evalmod()}} for generating \code{S3} objects with
performance evaluation metrics. \code{\link[=part]{part()}} for calculation of
pAUCs. \code{\link[=auc]{auc()}} for retrieving a dataset of AUCs, and for the
baseline a full area is read against.
}
