% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/g_fortify.R
\name{fortify.curves}
\alias{fortify.curves}
\title{Convert a curves object to a data frame for ggplot2.}
\usage{
fortify.curves(model, data = NULL, ...)
}
\arguments{
\item{model}{\code{curves} object generated by
\code{\link{evalmod}} or \code{\link{create_curves}}.}

\item{data}{Not used by this method.}

\item{...}{Further arguments (ignored).}
}
\value{
\code{fortify} retruns a data frame for \code{ggplot2}.
}
\description{
\code{fortify} takes a \code{curves} object generated by
\code{\link{evalmod}} or \code{\link{create_curves}}
and convert it to a data frame .
\code{fortify} requires \code{\link[ggplot2]{ggplot2}}.
}
\examples{
library(ggplot2)
data(B500)
curves <- create_curves(scores = B500$good_er_scores,
                        obslabs = B500$labels)
df <- fortify(curves)
p_roc <- ggplot2::ggplot(subset(df, group == "ROC"), aes(x = x, y = y))
p_roc <- p_roc + geom_line()
p_roc

p_prc <- ggplot2::ggplot(subset(df, group == "PRC"), aes(x = x, y = y))
p_prc <- p_prc + geom_line()
p_prc
}

