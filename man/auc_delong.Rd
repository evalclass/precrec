% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_auc_delong.R
\name{auc_delong}
\alias{auc_delong}
\title{DeLong's standard error for the ROC AUC}
\usage{
auc_delong(mdat, scores = NULL, labels = NULL, ...)
}
\arguments{
\item{mdat}{An \code{mdata} object created by \code{\link[=mmdata]{mmdata()}} holding exactly one
dataset. It can be omitted when \code{scores} and \code{labels} are given.}

\item{scores}{A numeric vector, matrix, array, data frame, or list of
scores. See \code{\link[=mmdata]{mmdata()}} for the accepted shapes.}

\item{labels}{A numeric, character, logical, or factor vector of observed
labels, or a list of such vectors.}

\item{...}{Further arguments passed to \code{\link[=mmdata]{mmdata()}} when \code{scores} and
\code{labels} are given instead of \code{mdat}.}
}
\value{
An object of class \code{aucdelong}: a data frame of one row per
model, with the columns \code{modnames}, \code{curvetypes}, \code{aucs} and \code{error},
the last being the standard error of the AUC. The covariance matrix of
the AUCs is attached as the \code{cov} attribute, and is what makes the
comparison in \code{\link[=auc_diff]{auc_diff()}} a paired one.
}
\description{
\code{auc_delong} calculates the ROC AUC of every model on one test set
together with the variance and covariance of those AUCs, exactly rather
than by resampling. \code{\link[=auc_ci]{auc_ci()}} turns the result into confidence
intervals and \code{\link[=auc_diff]{auc_diff()}} into comparisons between models.
}
\details{
The ROC AUC is a Mann-Whitney U statistic, so its variance follows from
the structural components of that statistic and needs no bootstrap. That
makes this the exact counterpart of \code{\link[=auc_boot]{auc_boot()}}: same input, same two
functions reading the result, no \code{boot_n}, no seed, nothing that changes
between two runs, and no floor under the p-value.
}
\section{The ROC AUC only}{


There is no precision-recall counterpart of this. The ROC AUC is a
U statistic - the probability that a random positive outranks a random
negative - and everything below rests on that. The precision-recall AUC
is not one, and the interpolated area \code{\link[=auc]{auc()}} reports is further from
being one still, so it has no analytic variance to report and does not
appear in the result.

\code{\link[=auc_boot]{auc_boot()}} remains the answer for the precision-recall AUC, and the
one to reach for whenever the precision-recall curve is the point. This
function is here because the ROC AUC is what a great many readers and
reviewers ask about by name, and because it is exact where the bootstrap
is approximate.
}

\section{How the variance is calculated}{


Write \code{m} for the positives and \code{n} for the negatives. Each positive \code{i}
and each negative \code{j} contributes a structural component - the share of
the other class it beats:

\if{html}{\out{<div class="sourceCode">}}\preformatted{V10[i] = (1 / n) * sum over j of psi(x[i], y[j])
V01[j] = (1 / m) * sum over i of psi(x[i], y[j])
}\if{html}{\out{</div>}}

where \code{psi} is 1 when the positive outranks the negative, 0.5 when they
are tied and 0 otherwise - the same half credit for a tie that the AUC
itself gives. The variance is then

\if{html}{\out{<div class="sourceCode">}}\preformatted{var(auc) = var(V10) / m + var(V01) / n
}\if{html}{\out{</div>}}

and for two models on the same test set the covariance is the same
expression with the covariance of their components. Both are computed
from midranks rather than from all \code{m * n} comparisons, so the cost is a
sort rather than a product.

The components are taken from the ranks \code{precrec} already assigns, so
ties and \code{NA} scores are handled exactly as they are everywhere else in
the package and the AUC reported here is the one \code{\link[=auc]{auc()}} reports.
}

\section{What it assumes}{


The variance is an \strong{asymptotic} one. It describes how the AUC would
move over repeated samples of the same size, and the interval built from
it is a normal one, so both get better as the test set grows. On a small
or badly imbalanced test set the percentile interval of \code{\link[=auc_boot]{auc_boot()}} is
the safer reading, and comparing the two is a cheap way to find out
whether the sample is large enough for this one.
}

\examples{

## One test set, two models
samps <- create_sim_samples(1, 100, 100, c("poor_er", "good_er"))
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]]
)

delong <- auc_delong(mdat)
delong

auc_ci(delong)
auc_diff(delong)

}
\seealso{
\code{\link[=auc_boot]{auc_boot()}} for the resampling counterpart, which also covers
the precision-recall AUC, \code{\link[=auc_ci]{auc_ci()}} for the intervals and
\code{\link[=auc_diff]{auc_diff()}} for comparing models.
}
