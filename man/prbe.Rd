% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_prbe.R
\name{prbe}
\alias{prbe}
\alias{prbe.curve_info}
\title{Calculate the precision-recall break-even point}
\usage{
prbe(curves)

\method{prbe}{curve_info}(curves)
}
\arguments{
\item{curves}{An \code{S3} object generated by \code{\link[=evalmod]{evalmod()}}.}
}
\value{
The \code{prbe} function returns a data frame with the columns
\code{modnames}, \code{dsids} and \code{prbe}. \code{prbe} is the common
value of precision and recall at the break-even point.

A curve can cross the diagonal more than once, and then the data frame
holds one row per crossing, ordered by recall. A curve that never
reaches equal precision and recall gets a single row of \code{NA}.
}
\description{
The \code{prbe} function finds the points of a precision-recall curve at
which precision and recall are equal. It takes an \code{S3} object
generated by \code{\link[=evalmod]{evalmod()}} and returns a data frame with one row per
break-even point.
}
\section{How this differs from ROCR}{


\code{ROCR::performance(pred, "prbe")} interpolates linearly between adjacent
raw precision-recall points to find the crossing. Linear interpolation
between precision-recall points is not correct, which is the reason
this package exists, so \code{prbe} reads the crossing off the curve
\code{\link[=evalmod]{evalmod()}} has already interpolated properly. The two agree wherever
the crossing falls on a point both of them hold, and differ where it
falls between two of them.
}

\examples{

##################################################
### Single model & single test dataset
###
samps <- create_sim_samples(1, 50, 50, "good_er")
sscurves <- evalmod(scores = samps[["scores"]], labels = samps[["labels"]])
prbe(sscurves)

##################################################
### Multiple models & multiple test datasets
###
samps2 <- create_sim_samples(2, 50, 50, c("poor_er", "good_er"))
mmdat <- mmdata(samps2[["scores"]], samps2[["labels"]],
  modnames = samps2[["modnames"]], dsids = samps2[["dsids"]]
)
mmcurves <- evalmod(mmdat, raw_curves = TRUE)
prbe(mmcurves)

}
\seealso{
\code{\link[=evalmod]{evalmod()}} for generating \code{S3} objects with performance
evaluation metrics. \code{\link[=auc]{auc()}} for the areas under the curves.
}
