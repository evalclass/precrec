% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_prob_metrics.R
\name{prob_metrics_ci}
\alias{prob_metrics_ci}
\title{Calculate CIs of the Brier score and the log loss}
\usage{
prob_metrics_ci(
  mdat,
  scores = NULL,
  labels = NULL,
  eps = 1e-15,
  alpha = 0.05,
  dtype = "normal",
  metrics = NULL,
  ...
)
}
\arguments{
\item{mdat}{An \code{S3} object created by the \code{\link[=mmdata]{mmdata()}} function.
It contains formatted scores and labels. The \code{prob_metrics}
function ignores \code{scores} and \code{labels} when \code{mdat}
is specified. These arguments are internally passed to the
\code{\link[=mmdata]{mmdata()}} function when \code{mdat} is unspecified. In that case,
both \code{scores} and \code{labels} must be at least specified.}

\item{scores}{A numeric dataset of predicted probabilities. It can be a
vector, a matrix, an array, a data frame, or a list.}

\item{labels}{A numeric, character, logical, or factor dataset
of observed labels. It can be a vector, a matrix, an array,
a data frame, or a list.}

\item{eps}{A numeric value used to clamp the scores away from \code{0}
and \code{1} before the log loss is calculated. A single confident and
wrong prediction would otherwise make the log loss infinite.}

\item{alpha}{A numeric value of the significant level (default: 0.05)}

\item{dtype}{A string to specify the distribution used for CI calculation.\tabular{ll}{
   \strong{dtype} \tab \strong{distribution} \cr
   normal (default) \tab Normal distribution \cr
   z \tab Normal distribution \cr
   t \tab t-distribution \cr
}}

\item{metrics}{A character vector of additional metrics to calculate.
The three metrics above are always returned; \code{"d2_brier"} and
\code{"d2_logloss"} are returned as well when they are named here, and
\code{"all"} asks for every metric the function knows.

A D2 score rescales a loss against the loss of the null model, the
one that predicts the observed prevalence for every case and ignores
the scores:

\code{D2 = 1 - loss(model) / loss(null)}

It is \code{1} for a perfect model and \code{0} for one that does no
better than the prevalence, and it is negative for a model that does
worse - which is a real result rather than an error, so it is not
clipped. A dataset holding a single class has a null loss of \code{0}
and so a D2 score of \code{NA}.}

\item{...}{These additional arguments are passed to \code{\link[=mmdata]{mmdata()}}
for data preparation.}
}
\value{
The \code{prob_metrics_ci} function returns a data frame with the
columns \code{modnames}, \code{metrics}, \code{mean}, \code{error},
\code{lower_bound}, \code{upper_bound}, and \code{n}.
}
\description{
The \code{prob_metrics_ci} function calculates the confidence intervals of
the Brier score and the log loss when multiple test datasets are
specified.
}
\examples{

##################################################
### Single model & multiple test datasets
###

## Create sample datasets with 100 positives and 100 negatives
samps <- create_sim_samples(4, 100, 100, "good_er")
mdat <- mmdata(samps[["scores"]], samps[["labels"]],
  modnames = samps[["modnames"]],
  dsids = samps[["dsids"]]
)

## Calculate the CIs
prob_metrics_ci(mdat)

}
\seealso{
\code{\link[=prob_metrics]{prob_metrics()}} for the per-dataset metrics themselves.
\code{\link[=auc_ci]{auc_ci()}} for the equivalent calculation on AUC scores.
}
